- name: variables
  hosts: web
  vars: 
    hostel: luckky PG/Hostel
    duration: 12months
    address: nacharam
    whom: friends
  tasks:
  - name: print information
    ansible.builtin.debug:
     msg: "I cam to hyderabad and staying in {{address}} with {{whom}} in {{hostel}} for {{duration}}"
